# AGENTS.md

## 一、项目定位

`ops-notify-gateway` 是统一运维通知网关，用于接收 Jenkins、Alertmanager 等运维事件，按 `channel` 路由到钉钉、飞书等通知平台。

第一版边界：

- 支持 Alertmanager 告警通知
- 支持 Jenkins 构建通知
- 支持 Generic 通用通知
- 支持钉钉、飞书发送
- 支持钉钉加签
- 不使用数据库
- 不提供管理后台
- 不实现异步队列、失败重试、通知记录查询

详细代码分层、目录、接口、配置、安全和测试规范见：`doc/项目开发规范.md`。

## 二、基本规则

- 默认使用中文回复，表达简洁明确。
- 修改代码前必须先阅读本文件和 `doc/项目开发规范.md`。
- 未经确认，不擅自扩展需求或增加功能。
- 不主动编写兼容性代码，除非用户明确要求。
- 不引入真实 webhook、secret、token、API Key 或数据库密码。
- 只做必要修改，不重构无关代码，不调整无关格式。

## 三、任务流程

简单任务可直接执行，需同时满足：

- 只涉及 1 个文件
- 需求明确
- 不修改接口协议、数据结构、核心流程、构建流程
- 不新增依赖

复杂任务必须先出方案并等待确认，满足任一条件即为复杂任务：

- 涉及多个模块
- 涉及 3 个及以上文件
- 修改接口协议、请求响应结构、配置结构或核心流程
- 新增依赖
- 调整架构或分层
- 引入数据库、队列、缓存、后台任务等新组件

需求不明确时必须先提问，禁止猜测实现。

## 四、工程约束

- 项目按 `api/service/repo/model/types/routes/middleware/pkg/sql` 分层规范执行。
- 第一版不使用数据库；未确认前不创建 `repo/`、`model/`、`sql/`。
- `pkg` 只放公共能力，不放 Jenkins、Alertmanager、channel 路由或通知编排业务逻辑。
- 配置通过 `config/` 文件管理，禁止提交真实私密配置。
- 新增依赖必须先说明用途、替代方案和风险，并等待确认。

## 五、文档同步

以下变更必须同步 `doc/` 或 `README.md`：

- 新增或修改接口
- 修改请求或响应字段
- 修改配置项
- 修改 channel 路由规则
- 修改钉钉、飞书签名或 payload 格式
- 修改部署、启动、验证方式

## 六、验证规范

纯文档修改：

```bash
git diff --check
```

Go 代码修改：

```bash
gofmt -w <changed-go-files>
go test ./...
git diff --check
```

涉及依赖变化：

```bash
go mod tidy
go test ./...
git diff --check
```

复杂逻辑或核心流程变化，增加：

```bash
go vet ./...
```

验证失败时必须先定位；失败与本次修改无关时，说明证据和影响范围。

## 七、Git 规则

- 默认不执行 `git commit`、`git push`。
- 仅当用户明确要求，或复杂任务确认方案中包含提交步骤时，才可提交。
- `git push` 必须单独确认。
- 提交前必须执行 `git status`、`git diff`、必要验证和敏感信息检查。
- 禁止提交构建产物、日志、真实配置、临时文件。
- 禁止使用 `git reset --hard` 等破坏性命令，除非用户明确要求。

## 八、结果输出

每次修改后必须说明：

- 修改文件列表
- 修改内容摘要
- 本次修改影响范围
- 是否兼容已有功能
- 是否需要联动修改
- 是否已更新文档
- 验证结果
- 提交结果
- 推送结果
